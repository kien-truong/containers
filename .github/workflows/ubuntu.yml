name: ubuntu

on:
  workflow_dispatch:
  schedule:
    - cron: '40 2 * * 0'
  push:
    branches:
      - main
    paths:
      - "bases/ubuntu/revision.json"

jobs:
  images-build:
    uses: kien-truong/containers/.github/workflows/collect-image-metadata.yml@main
    with:
      path: bases/ubuntu
      schedule: ${{ github.event.schedule }}
    secrets: inherit
